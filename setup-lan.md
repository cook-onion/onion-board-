# 局域网对战配置指南

## 🎯 问题描述

本地电脑可以打开两个浏览器进行在线对战，但同一局域网的两台电脑无法进行在线对战。

## 🔧 解决步骤

### 步骤 1：获取服务端电脑的局域网 IP 地址

在运行服务端的电脑上执行以下命令：

**Windows:**

```cmd
ipconfig
```

**Mac/Linux:**

```bash
ifconfig
```

找到类似 `192.168.1.100` 或 `10.0.0.100` 的 IP 地址（不是 127.0.0.1 或 localhost）

### 步骤 2：修改前端配置

在前端项目根目录创建或修改 `.env.local` 文件：

```env
# 将下面的IP地址替换为步骤1中获取的实际IP地址
VITE_SERVER_URL=http://192.168.1.100:10001
```

### 步骤 3：配置 Windows 防火墙（如果服务端是 Windows）

1. 打开"控制面板" → "系统和安全" → "Windows Defender 防火墙"
2. 点击"高级设置"
3. 选择"入站规则" → "新建规则"
4. 选择"端口" → "下一步"
5. 选择"TCP"，输入端口号 `10001` → "下一步"
6. 选择"允许连接" → "下一步"
7. 全选配置文件 → "下一步"
8. 输入名称如"五子棋游戏端口" → "完成"

### 步骤 4：重启服务

1. 停止后端服务（Ctrl+C）
2. 重新启动后端服务：

   ```bash
   cd D:\myselfProject\onion-board-sever
   npm start
   ```

3. 停止前端服务（Ctrl+C）
4. 重新启动前端服务：
   ```bash
   cd D:\myselfProject\onion-board-
   npm run dev
   ```

### 步骤 5：测试连接

1. 在服务端电脑浏览器中访问：`http://localhost:10001`

   - 应该看到"Server is running and ready for WebSocket connections."

2. 在客户端电脑浏览器中访问：`http://192.168.1.100:10001`（替换为实际 IP）

   - 应该看到相同的消息

3. 在客户端电脑浏览器中访问前端：`http://192.168.1.100:10000`（替换为实际 IP）
   - 应该能正常打开游戏界面

## 🚨 常见问题

### Q1: 修改了配置还是连不上？

**A:** 检查以下几点：

- 确认 IP 地址是否正确
- 确认防火墙是否已配置
- 确认两台电脑在同一局域网内
- 尝试关闭杀毒软件的网络保护功能

### Q2: 如何确认服务端正在运行？

**A:** 在服务端电脑浏览器中访问 `http://localhost:10001`，应该看到服务器运行信息。

### Q3: 前端也需要配置防火墙吗？

**A:** 通常不需要，但如果前端也在局域网中提供服务，可能需要开放 10000 端口。

### Q4: 可以使用手机连接吗？

**A:** 可以！手机连接同一 WiFi 后，在手机浏览器中访问 `http://192.168.1.100:10000`（替换为实际 IP）。

## 🎮 使用网络诊断工具

在游戏主菜单中点击"网络诊断"按钮，可以：

- 检查当前服务器连接状态
- 查看详细的配置指南
- 获取常见问题的解决方案

## 📝 配置文件示例

**.env.local 文件内容示例：**

```env
# 局域网配置 - 请替换为实际IP地址
VITE_SERVER_URL=http://192.168.1.100:10001
```

**验证配置是否正确：**

1. 重启前端服务后，在浏览器开发者工具的 Console 中应该能看到连接的服务器地址
2. 网络诊断工具显示"连接正常"

## 🎉 成功标志

配置成功后，你应该能够：

- 在服务端电脑上正常游戏
- 在局域网内其他电脑上访问游戏
- 两台不同电脑之间进行在线对战
- 手机等移动设备也能连接游戏
